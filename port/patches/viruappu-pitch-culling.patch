diff --git a/include/cpu/mode1.h b/include/cpu/mode1.h
index d64129e..e664428 100644
--- a/include/cpu/mode1.h
+++ b/include/cpu/mode1.h
@@ -112,6 +112,9 @@ typedef struct VirtuaPPUMode1GbaMemory {
 
 void virtuappu_mode1_bind_gba_memory(const VirtuaPPUMode1GbaMemory *memory);
 void virtuappu_mode1_get_bound_gba_memory(VirtuaPPUMode1GbaMemory *memory);
+void virtuappu_mode1_set_frame_geometry(const PPUMemory *ppu);
+int virtuappu_mode1_frame_width(void);
+int virtuappu_mode1_frame_pitch(void);
 uint16_t virtuappu_mode1_io_read16(uint16_t offset);
 uint32_t virtuappu_mode1_io_read32(uint16_t offset);
 uint32_t virtuappu_mode1_rgb555_to_abgr8888(uint16_t color);
diff --git a/include/ppu_memory.h b/include/ppu_memory.h
index b040817..29f1006 100644
--- a/include/ppu_memory.h
+++ b/include/ppu_memory.h
@@ -3,7 +3,12 @@
 #include <stdint.h>
 
 typedef struct PPUMemory {
+    /* Visible pixels per scanline. Rendering loops cull to this width. */
     uint16_t frame_width;
+    /* Pixels between row starts in virtuappu_frame_buffer. A value of 0
+     * means tightly packed rows (`frame_width`). Kept separate so ports can
+     * render a 240-wide viewport into a wider fixed-pitch buffer. */
+    uint16_t frame_pitch;
     uint8_t mode;
     uint8_t reserved;
 } PPUMemory;
diff --git a/src/mode0.c b/src/mode0.c
index 915a28a..a866caf 100644
--- a/src/mode0.c
+++ b/src/mode0.c
@@ -183,11 +183,14 @@ static void mode0_render_bg(uint8_t index, uint64_t *opaque_mask, uint32_t *scan
 static void mode0_composite_and_oam(const uint32_t *scanline_layers, const PPUMemory *ppu, size_t line)
 {
     const size_t width = (size_t)ppu->frame_width;
+    const size_t pitch = (ppu->frame_pitch != 0u && ppu->frame_pitch >= ppu->frame_width)
+                             ? (size_t)ppu->frame_pitch
+                             : width;
     const uint32_t *bg0 = &scanline_layers[0];
     size_t x;
 
     for (x = 0; x < width; ++x) {
-        virtuappu_frame_buffer[line * width + x] = bg0[x];
+        virtuappu_frame_buffer[line * pitch + x] = bg0[x];
     }
 }
 
@@ -202,7 +205,11 @@ void virtuappu_mode0_render_frame(const PPUMemory *ppu)
     }
 
     width = (size_t)ppu->frame_width;
-    padded_width = width + (width % 32u);
+    if (ppu->frame_pitch != 0u &&
+        (ppu->frame_pitch < ppu->frame_width || ppu->frame_pitch > VIRTUAPPU_MAX_FRAME_WIDTH)) {
+        return;
+    }
+    padded_width = (width + 31u) & ~31u;
 
 #ifdef USE_OPENMP
 #pragma omp parallel for
diff --git a/src/mode1.c b/src/mode1.c
index 528585a..8ac3bb9 100644
--- a/src/mode1.c
+++ b/src/mode1.c
@@ -47,6 +47,46 @@ static VirtuaPPUMode1GbaMemory mode1_memory = {
     mode1_default_oam_mem
 };
 
+static int mode1_frame_width = MODE1_GBA_WIDTH;
+static int mode1_frame_pitch = MODE1_GBA_WIDTH;
+
+void virtuappu_mode1_set_frame_geometry(const PPUMemory *ppu)
+{
+    int width = MODE1_GBA_WIDTH;
+    int pitch = MODE1_GBA_WIDTH;
+
+    if (ppu != NULL && ppu->frame_width != 0u) {
+        width = (int)ppu->frame_width;
+    }
+    if (width < 1) {
+        width = 1;
+    } else if (width > MODE1_GBA_WIDTH) {
+        width = MODE1_GBA_WIDTH;
+    }
+
+    if (ppu != NULL && ppu->frame_pitch != 0u) {
+        pitch = (int)ppu->frame_pitch;
+    }
+    if (pitch < width) {
+        pitch = width;
+    } else if (pitch > VIRTUAPPU_MAX_FRAME_WIDTH) {
+        pitch = VIRTUAPPU_MAX_FRAME_WIDTH;
+    }
+
+    mode1_frame_width = width;
+    mode1_frame_pitch = pitch;
+}
+
+int virtuappu_mode1_frame_width(void)
+{
+    return mode1_frame_width;
+}
+
+int virtuappu_mode1_frame_pitch(void)
+{
+    return mode1_frame_pitch;
+}
+
 static const uint8_t mode1_obj_widths[3][4] = {
     {8, 16, 32, 64},
     {16, 32, 32, 64},
@@ -340,29 +380,28 @@ void virtuappu_mode1_render_text_bg_line(int bg_index, int line, uint32_t *line_
     int tile_row = src_y / 8;
     int pixel_y = src_y % 8;
     int x;
+    const int frame_width = mode1_frame_width;
     /* Widescreen Option A: 32-tile BGs have valid VRAM tile data only
-     * within the native 240 px; render up to MODE1_GBA_WIDTH but read the
-     * reveal columns (x >= MODE1_GBA_BG_CLIP_X) from the port-side shadow.
-     * shadow==NULL => clip at 240 (composite force-blacks past it).
-     * 64-tile BGs span the full source, so render them straight from VRAM. */
+     * within the native 240 px. Cull the line to the current visible frame
+     * width, but keep the fixed framebuffer pitch separate for presentation. */
     int render_max_x =
-        (map_width_tiles >= 64) ? MODE1_GBA_WIDTH
+        (map_width_tiles >= 64) ? frame_width
                                 : ((virtuappu_mode1_ws_shadow[bg_index] != NULL)
-                                       ? MODE1_GBA_WIDTH
+                                       ? frame_width
                                        : MODE1_GBA_BG_CLIP_X);
-    if (render_max_x > MODE1_GBA_WIDTH) render_max_x = MODE1_GBA_WIDTH;
+    if (render_max_x > frame_width) render_max_x = frame_width;
     const bool ws_shadow_active =
         (map_width_tiles < 64) && (virtuappu_mode1_ws_shadow[bg_index] != NULL);
     const int ws_shadow_base = virtuappu_mode1_ws_shadow_base_tile[bg_index];
     uint16_t * const ws_shadow = virtuappu_mode1_ws_shadow[bg_index];
     const bool ws_hud_right_anchor =
         (bg_index == 0) && (virtuappu_mode1_ws_hud_right_anchor != 0) &&
-        (MODE1_GBA_WIDTH > MODE1_GBA_BG_CLIP_X);
+        (frame_width > MODE1_GBA_BG_CLIP_X);
     const int ws_hud_right_dst_x =
-        MODE1_GBA_WIDTH - (MODE1_GBA_BG_CLIP_X - MODE1_WS_HUD_RIGHT_NATIVE_X);
+        frame_width - (MODE1_GBA_BG_CLIP_X - MODE1_WS_HUD_RIGHT_NATIVE_X);
 
     if (ws_hud_right_anchor) {
-        render_max_x = MODE1_GBA_WIDTH;
+        render_max_x = frame_width;
     }
     /* N64 perf: the tilemap screen-entry read + its address math are constant
      * across a tile's 8 px; cache them and recompute only when the tile cell
@@ -378,7 +417,7 @@ void virtuappu_mode1_render_text_bg_line(int bg_index, int line, uint32_t *line_
         int sample_x = x;
         if (ws_hud_right_anchor) {
             if (x >= ws_hud_right_dst_x) {
-                sample_x = x - (MODE1_GBA_WIDTH - MODE1_GBA_BG_CLIP_X);
+                sample_x = x - (frame_width - MODE1_GBA_BG_CLIP_X);
             } else if (x >= MODE1_WS_HUD_RIGHT_NATIVE_X) {
                 continue;
             }
@@ -457,8 +496,8 @@ void virtuappu_mode1_render_obj_line(int line, bool obj_1d, uint32_t *line_buffe
     const uint32_t obj_tile_base = 0x10000u;
     int i;
 
-    memset(virtuappu_mode1_obj_semitrans, 0, sizeof(virtuappu_mode1_obj_semitrans));
-    memset(virtuappu_mode1_obj_window, 0, sizeof(virtuappu_mode1_obj_window));
+    memset(virtuappu_mode1_obj_semitrans, 0, (size_t)mode1_frame_width);
+    memset(virtuappu_mode1_obj_window, 0, (size_t)mode1_frame_width);
 
     for (i = MODE1_GBA_OAM_COUNT - 1; i >= 0; --i) {
         Mode1OAMAttr attr;
@@ -485,6 +524,9 @@ void virtuappu_mode1_render_obj_line(int line, bool obj_1d, uint32_t *line_buffe
         int sprite_half_height;
         int input_rel_y;
         int sx;
+        int sx_start;
+        int sx_end;
+        int viewport_width;
 
         attr.attr0 = mode1_memory.oam_mem[i * 4];
         attr.attr1 = mode1_memory.oam_mem[i * 4 + 1];
@@ -516,9 +558,24 @@ void virtuappu_mode1_render_obj_line(int line, bool obj_1d, uint32_t *line_buffe
         }
 
         obj_x = mode1_oam_x(attr);
-        if (obj_x >= MODE1_GBA_WIDTH) {
+        viewport_width = mode1_frame_width;
+        if (viewport_width > MODE1_GBA_VIEWPORT_X) {
+            viewport_width = MODE1_GBA_VIEWPORT_X;
+        }
+        if (obj_x >= mode1_frame_width) {
             obj_x -= 512;
         }
+        sx_start = 0;
+        sx_end = bounds_width;
+        if (obj_x + bounds_width <= 0 || obj_x >= viewport_width) {
+            continue;
+        }
+        if (obj_x < 0) {
+            sx_start = -obj_x;
+        }
+        if (obj_x + sx_end > viewport_width) {
+            sx_end = viewport_width - obj_x;
+        }
 
         bpp8 = mode1_oam_bpp8(attr);
         priority = mode1_oam_priority(attr);
@@ -539,7 +596,10 @@ void virtuappu_mode1_render_obj_line(int line, bool obj_1d, uint32_t *line_buffe
         sprite_half_height = obj_height / 2;
         input_rel_y = line - obj_y - half_height;
 
-        for (sx = 0; sx < bounds_width; ++sx) {
+        /* Horizontal frustum culling is done once per sprite. Partially visible
+         * sprites enter the loop at the first on-screen column, so the hot
+         * pixel path has no screen_x bounds branch. */
+        for (sx = sx_start; sx < sx_end; ++sx) {
             int screen_x = obj_x + sx;
             int tex_x;
             int tex_y;
@@ -551,15 +611,6 @@ void virtuappu_mode1_render_obj_line(int line, bool obj_1d, uint32_t *line_buffe
             uint8_t color_index;
             uint16_t rgb555;
 
-            /* PORT_WIDESCREEN_SPIKE: clip OAM at the configured widescreen
-             * viewport. At <=256 widescreen, MODE1_GBA_VIEWPORT_X equals
-             * MODE1_GBA_WIDTH so all on-screen sprites render. Above 256,
-             * the viewport stays at 240 to keep parked off-screen sprites
-             * from leaking into the pillarbox region. */
-            if (screen_x < 0 || screen_x >= MODE1_GBA_VIEWPORT_X) {
-                continue;
-            }
-
             if (is_affine) {
                 int input_rel_x = sx - half_width;
                 tex_x = ((pa * input_rel_x + pb * input_rel_y) >> 8) + sprite_half_width;
@@ -685,6 +736,9 @@ void virtuappu_mode1_composite_line(
     uint8_t objwin_ctrl = (uint8_t)((winout >> 8u) & 0x3Fu);
     int i;
     int x;
+    const int frame_width = mode1_frame_width;
+    uint32_t * const out_row =
+        &virtuappu_frame_buffer[(size_t)line * (size_t)mode1_frame_pitch];
 
     (void)bg_priority;
 
@@ -698,14 +752,14 @@ void virtuappu_mode1_composite_line(
         evy = 16;
     }
 
-    if (win0_right > MODE1_GBA_WIDTH) {
-        win0_right = MODE1_GBA_WIDTH;
+    if (win0_right > frame_width) {
+        win0_right = frame_width;
     }
     if (win0_bottom > MODE1_GBA_HEIGHT) {
         win0_bottom = MODE1_GBA_HEIGHT;
     }
-    if (win1_right > MODE1_GBA_WIDTH) {
-        win1_right = MODE1_GBA_WIDTH;
+    if (win1_right > frame_width) {
+        win1_right = frame_width;
     }
     if (win1_bottom > MODE1_GBA_HEIGHT) {
         win1_bottom = MODE1_GBA_HEIGHT;
@@ -750,7 +804,7 @@ void virtuappu_mode1_composite_line(
         bg_order[j + 1] = key;
     }
 
-    for (x = 0; x < MODE1_GBA_WIDTH; ++x) {
+    for (x = 0; x < frame_width; ++x) {
         uint8_t win_ctrl = 0x3Fu;
         bool visible_bg[MODE1_GBA_BG_COUNT];
         bool visible_obj;
@@ -881,10 +935,9 @@ void virtuappu_mode1_composite_line(
                     break;
                 }
             }
-            virtuappu_frame_buffer[(size_t)line * MODE1_GBA_WIDTH + (size_t)x] =
-                any_bg_drew_here ? top_color : 0xFF000000u;
+            out_row[x] = any_bg_drew_here ? top_color : 0xFF000000u;
         } else {
-            virtuappu_frame_buffer[(size_t)line * MODE1_GBA_WIDTH + (size_t)x] = top_color;
+            out_row[x] = top_color;
         }
     }
 }
@@ -911,7 +964,11 @@ void virtuappu_mode1_render_affine_obj_overlay(uint32_t *dst, int dst_w, int dst
     if (dst == NULL || scale <= 1) {
         return;
     }
-    if (dst_w != MODE1_GBA_WIDTH * scale || dst_h != MODE1_GBA_HEIGHT * scale) {
+    if ((dst_w % scale) != 0 || dst_h != MODE1_GBA_HEIGHT * scale) {
+        return;
+    }
+    const int viewport_width = dst_w / scale;
+    if (viewport_width < 1 || viewport_width > MODE1_GBA_WIDTH) {
         return;
     }
 
@@ -951,7 +1008,32 @@ void virtuappu_mode1_render_affine_obj_overlay(uint32_t *dst, int dst_w, int dst
         int obj_y = mode1_oam_y(attr);
         if (obj_y >= MODE1_GBA_HEIGHT) obj_y -= 256;
         int obj_x = mode1_oam_x(attr);
-        if (obj_x >= MODE1_GBA_WIDTH)  obj_x -= 512;
+        if (obj_x >= viewport_width)  obj_x -= 512;
+
+        int sy_sub_start = 0;
+        int sy_sub_end = bounds_height * scale;
+        int sx_sub_start = 0;
+        int sx_sub_end = bounds_width * scale;
+        const int sprite_top_sub = obj_y * scale;
+        const int sprite_left_sub = obj_x * scale;
+        if (sprite_top_sub + sy_sub_end <= 0 || sprite_top_sub >= dst_h) {
+            continue;
+        }
+        if (sprite_top_sub < 0) {
+            sy_sub_start = -sprite_top_sub;
+        }
+        if (sprite_top_sub + sy_sub_end > dst_h) {
+            sy_sub_end = dst_h - sprite_top_sub;
+        }
+        if (sprite_left_sub + sx_sub_end <= 0 || sprite_left_sub >= dst_w) {
+            continue;
+        }
+        if (sprite_left_sub < 0) {
+            sx_sub_start = -sprite_left_sub;
+        }
+        if (sprite_left_sub + sx_sub_end > dst_w) {
+            sx_sub_end = dst_w - sprite_left_sub;
+        }
 
         bool bpp8 = mode1_oam_bpp8(attr);
         uint16_t base_tile = mode1_oam_tile_index(attr);
@@ -968,21 +1050,18 @@ void virtuappu_mode1_render_affine_obj_overlay(uint32_t *dst, int dst_w, int dst
         int sprite_half_w     = obj_width  / 2;
         int sprite_half_h     = obj_height / 2;
 
-        /* Iterate output sub-pixels. (sx_sub, sy_sub) are in 1/scale source-
-         * pixel units; the matrix math at scale=1 reproduces the integer
-         * mode1_render_obj_line behaviour exactly. */
-        for (int sy_sub = 0; sy_sub < bounds_height * scale; ++sy_sub) {
+        /* Iterate only the visible output sub-pixels. The frustum clamp above
+         * removes both the vertical and horizontal bounds branches from the
+         * hot affine sprite loop. */
+        for (int sy_sub = sy_sub_start; sy_sub < sy_sub_end; ++sy_sub) {
             /* line_sub = output row = sprite-top output row + sub-row within sprite. */
-            int line_sub = obj_y * scale + sy_sub;
-            if (line_sub < 0 || line_sub >= MODE1_GBA_HEIGHT * scale) continue;
+            int line_sub = sprite_top_sub + sy_sub;
 
             /* input_rel_y in (1/scale) source-pixel units. */
             int input_rel_y_subS = sy_sub - half_height * scale;
 
-            for (int sx_sub = 0; sx_sub < bounds_width * scale; ++sx_sub) {
-                int screen_x_sub = obj_x * scale + sx_sub;
-                if (screen_x_sub < 0 || screen_x_sub >= MODE1_GBA_WIDTH * scale) continue;
-
+            for (int sx_sub = sx_sub_start; sx_sub < sx_sub_end; ++sx_sub) {
+                int screen_x_sub = sprite_left_sub + sx_sub;
                 int input_rel_x_subS = sx_sub - half_width * scale;
                 /* tex_x / tex_y in source-pixel units. pa is 8.8 fixed,
                  * input_rel_*_subS is 1/scale source-pixels, so the product
@@ -1036,11 +1115,15 @@ void virtuappu_mode1_render_frame(const PPUMemory *ppu)
     uint16_t dispcnt;
     int line;
 
-    (void)ppu;
+    virtuappu_mode1_set_frame_geometry(ppu);
 
     dispcnt = virtuappu_mode1_io_read16(MODE1_IO_DISPCNT);
     if ((dispcnt & MODE1_DISP_FORCED_BLANK) != 0u) {
-        memset(virtuappu_frame_buffer, 0xFF, MODE1_GBA_WIDTH * MODE1_GBA_HEIGHT * sizeof(uint32_t));
+        for (line = 0; line < MODE1_GBA_HEIGHT; ++line) {
+            memset(&virtuappu_frame_buffer[(size_t)line * (size_t)mode1_frame_pitch],
+                   0xFF,
+                   (size_t)mode1_frame_width * sizeof(uint32_t));
+        }
         return;
     }
 
@@ -1082,10 +1165,12 @@ void virtuappu_mode1_render_frame(const PPUMemory *ppu)
 
         virtuappu_mode1_io_thread_override = io_snapshots[line];
 
-        memset(bg_layers, 0, sizeof(bg_layers));
-        memset(bg_priority, 0, sizeof(bg_priority));
-        memset(obj_layer, 0, sizeof(obj_layer));
-        memset(obj_priority, 0xFF, sizeof(obj_priority));
+        for (int b = 0; b < MODE1_GBA_BG_COUNT; ++b) {
+            memset(bg_layers[b], 0, (size_t)mode1_frame_width * sizeof(uint32_t));
+            memset(bg_priority[b], 0, (size_t)mode1_frame_width);
+        }
+        memset(obj_layer, 0, (size_t)mode1_frame_width * sizeof(uint32_t));
+        memset(obj_priority, 0xFF, (size_t)mode1_frame_width);
 
         if ((line_dispcnt & MODE1_DISP_BG0_ON) != 0u) {
             virtuappu_mode1_render_text_bg_line(0, line, bg_layers[0], bg_priority[0]);
diff --git a/src/mode2.c b/src/mode2.c
index 62f6170..5a0a0da 100644
--- a/src/mode2.c
+++ b/src/mode2.c
@@ -11,11 +11,17 @@ void virtuappu_mode2_render_frame(const PPUMemory *ppu)
     uint16_t dispcnt;
     int line;
 
-    (void)ppu;
+    virtuappu_mode1_set_frame_geometry(ppu);
+    const int frame_width = virtuappu_mode1_frame_width();
+    const int frame_pitch = virtuappu_mode1_frame_pitch();
 
     dispcnt = virtuappu_mode1_io_read16(MODE1_IO_DISPCNT);
     if ((dispcnt & MODE1_DISP_FORCED_BLANK) != 0u) {
-        memset(virtuappu_frame_buffer, 0xFF, MODE1_GBA_WIDTH * MODE1_GBA_HEIGHT * sizeof(uint32_t));
+        for (line = 0; line < MODE1_GBA_HEIGHT; ++line) {
+            memset(&virtuappu_frame_buffer[(size_t)line * (size_t)frame_pitch],
+                   0xFF,
+                   (size_t)frame_width * sizeof(uint32_t));
+        }
         return;
     }
 
@@ -58,10 +64,12 @@ void virtuappu_mode2_render_frame(const PPUMemory *ppu)
         VirtuaPPUMode1GbaMemory memory;
         bool obj_1d = (dispcnt & MODE1_DISP_OBJ_1D) != 0u;
 
-        memset(bg_layers, 0, sizeof(bg_layers));
-        memset(bg_priority, 0, sizeof(bg_priority));
-        memset(obj_layer, 0, sizeof(obj_layer));
-        memset(obj_priority, 0xFF, sizeof(obj_priority));
+        for (int b = 0; b < MODE1_GBA_BG_COUNT; ++b) {
+            memset(bg_layers[b], 0, (size_t)frame_width * sizeof(uint32_t));
+            memset(bg_priority[b], 0, (size_t)frame_width);
+        }
+        memset(obj_layer, 0, (size_t)frame_width * sizeof(uint32_t));
+        memset(obj_priority, 0xFF, (size_t)frame_width);
 
         if ((dispcnt & MODE1_DISP_BG0_ON) != 0u) {
             virtuappu_mode1_render_text_bg_line(0, line, bg_layers[0], bg_priority[0]);
@@ -123,7 +131,7 @@ void virtuappu_mode2_render_frame(const PPUMemory *ppu)
             affine_prev_io_x = ref_x;
             affine_prev_io_y = ref_y;
 
-            for (x = 0; x < MODE1_GBA_WIDTH; ++x) {
+            for (x = 0; x < frame_width; ++x) {
                 int32_t tex_x = affine_internal_x + pa * x;
                 int32_t tex_y = affine_internal_y + pc * x;
                 int32_t src_x = tex_x >> 8;
diff --git a/src/mode7.c b/src/mode7.c
index 50fca29..a3ff1ad 100644
--- a/src/mode7.c
+++ b/src/mode7.c
@@ -129,14 +129,17 @@ void virtuappu_mode7_render_frame(const PPUMemory *ppu)
     const Mode7Layout *layout = mode7_get_layout();
     const Mode7GBRegs *regs = &layout->regs;
     uint8_t y;
-
-    (void)ppu;
+    const size_t pitch =
+        (ppu != NULL && ppu->frame_pitch >= MODE7_GB_SCREEN_WIDTH)
+            ? (size_t)ppu->frame_pitch
+            : (size_t)MODE7_GB_SCREEN_WIDTH;
 
     if ((regs->lcdc & MODE7_LCDC_ENABLE) == 0u) {
         uint32_t clear_color = mode7_palette_color(regs->bgp, 0u);
-        size_t i;
-        for (i = 0; i < (size_t)MODE7_GB_SCREEN_WIDTH * MODE7_GB_SCREEN_HEIGHT; ++i) {
-            virtuappu_frame_buffer[i] = clear_color;
+        for (y = 0u; y < MODE7_GB_SCREEN_HEIGHT; ++y) {
+            for (uint8_t x = 0u; x < MODE7_GB_SCREEN_WIDTH; ++x) {
+                virtuappu_frame_buffer[(size_t)y * pitch + x] = clear_color;
+            }
         }
         return;
     }
@@ -236,7 +239,7 @@ void virtuappu_mode7_render_frame(const PPUMemory *ppu)
                 }
             }
 
-            virtuappu_frame_buffer[(size_t)y * MODE7_GB_SCREEN_WIDTH + x] = final_color;
+            virtuappu_frame_buffer[(size_t)y * pitch + x] = final_color;
         }
     }
 }
