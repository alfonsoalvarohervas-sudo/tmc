name: _build

on:
  workflow_call:
    inputs:
      publish_mode:
        description: "artifact for CI artifacts, release for tag artifacts"
        required: true
        type: string
      version_label:
        description: "Date+SHA label (CI) or tag name (release)"
        required: true
        type: string
      os_filter:
        description: "Optional OS filter for the build matrix"
        required: false
        default: all
        type: string
    secrets:
      ASSETS_TOKEN:
        required: true
      PRIVATE_SUBMODULES_TOKEN:
        required: false

jobs:
  build:
    name: Build ${{ matrix.region }} on ${{ matrix.os_name }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            os_name: linux
            region: usa
            flag: --usa
            exe_name: tmc_pc
          - runner: windows-latest
            os_name: windows
            region: usa
            flag: --usa
            exe_name: tmc_pc.exe
    steps:
      - name: Checkout source
        if: ${{ inputs.os_filter == 'all' || inputs.os_filter == matrix.os_name }}
        uses: actions/checkout@v4
        with:
          submodules: false
          clean: false
      - name: Checkout private submodules
        if: ${{ inputs.os_filter == 'all' || inputs.os_filter == matrix.os_name }}
        shell: bash
        env:
          SUBMODULES_TOKEN: ${{ secrets.PRIVATE_SUBMODULES_TOKEN }}
        run: |
          if [ -n "$SUBMODULES_TOKEN" ]; then
            url_cfg="url.https://x-access-token:${SUBMODULES_TOKEN}@github.com/.insteadOf"
            git -c "$url_cfg=https://github.com/" -c "$url_cfg=https://github.com" \
                -c "$url_cfg=git@github.com:" -c "$url_cfg=ssh://git@github.com/" \
                submodule sync -- libs/ViruaPPU libs/VirtuaAPU
            git -c "$url_cfg=https://github.com/" -c "$url_cfg=https://github.com" \
                -c "$url_cfg=git@github.com:" -c "$url_cfg=ssh://git@github.com/" \
                submodule update --init --depth 1 libs/ViruaPPU libs/VirtuaAPU
          else
            git submodule sync -- libs/ViruaPPU libs/VirtuaAPU
            git submodule update --init --depth 1 libs/ViruaPPU libs/VirtuaAPU
          fi

      - name: Install Linux dependencies
        if: ${{ (inputs.os_filter == 'all' || inputs.os_filter == matrix.os_name) && matrix.os_name == 'linux' }}
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            curl libpng-dev libfmt-dev nlohmann-json3-dev \
            libx11-dev libxext-dev libxrender-dev libxcursor-dev \
            xutils-dev xorg-dev autoconf automake libtool meson ninja-build

      - name: Install MinGW Windows
        if: ${{ (inputs.os_filter == 'all' || inputs.os_filter == matrix.os_name) && matrix.os_name == 'windows' }}
        shell: powershell
        run: |
          choco install mingw -y --no-progress

          $candidates = @(
            "C:\ProgramData\mingw64\mingw64\bin",
            "C:\ProgramData\mingw64\bin",
            "C:\ProgramData\chocolatey\lib\mingw\tools\install\mingw64\mingw64\bin",
            "C:\ProgramData\chocolatey\lib\mingw\tools\install\mingw64\bin",
            "C:\tools\mingw64\bin"
          )

          $mingw = $null
          foreach ($path in $candidates) {
            if (Test-Path "$path\gcc.exe") {
              $mingw = $path
              break
            }
          }

          if ($null -eq $mingw) {
            Write-Host "Known MinGW paths not found, searching gcc.exe..."
            $gcc = Get-ChildItem -Path "C:\ProgramData" -Filter gcc.exe -Recurse -ErrorAction SilentlyContinue |
              Where-Object { $_.FullName -match "mingw.*\\bin\\gcc\.exe$" } |
              Select-Object -First 1

            if ($null -ne $gcc) {
              $mingw = Split-Path $gcc.FullName
            }
          }

          if ($null -eq $mingw) {
            Write-Error "MinGW gcc.exe not found"
            exit 1
          }

          Write-Host "Using MinGW from: $mingw"
          echo $mingw | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          $env:PATH = "$mingw;$env:PATH"

          gcc --version
          g++ --version

      - name: Setup xmake
        if: ${{ inputs.os_filter == 'all' || inputs.os_filter == matrix.os_name }}
        uses: xmake-io/github-action-setup-xmake@v1
        with:
          xmake-version: latest

      - name: Checkout private assets repository
        if: ${{ (inputs.os_filter == 'all' || inputs.os_filter == matrix.os_name) && vars.ASSETS_REPO != '' }}
        uses: actions/checkout@v4
        with:
          repository: ${{ vars.ASSETS_REPO }}
          token: ${{ secrets.ASSETS_TOKEN }}
          path: _assets

      - name: Copy ROMs into workspace
        if: ${{ inputs.os_filter == 'all' || inputs.os_filter == matrix.os_name }}
        shell: bash
        run: |
          if [ -f "_assets/baserom.gba" ]; then
            cp _assets/baserom.gba ./baserom.gba
          fi
          if [ -f "_assets/baserom_eu.gba" ]; then
            cp _assets/baserom_eu.gba ./baserom_eu.gba
          fi
          test -f ./baserom.gba
          test -f ./baserom_eu.gba

      - name: Build selected region (non-interactive, Linux)
        if: ${{ (inputs.os_filter == 'all' || inputs.os_filter == matrix.os_name) && matrix.os_name == 'linux' }}
        run: python build.py ${{ matrix.flag }} --slim

      - name: Build selected region (non-interactive, Windows MinGW)
        if: ${{ (inputs.os_filter == 'all' || inputs.os_filter == matrix.os_name) && matrix.os_name == 'windows' }}
        env:
          TMC_XMAKE_TOOLCHAIN: mingw
          PYTHONUTF8: "1"
          PYTHONIOENCODING: "utf-8"
        run: python build.py ${{ matrix.flag }} --slim

      - name: Prepare artifact payload
        if: ${{ inputs.os_filter == 'all' || inputs.os_filter == matrix.os_name }}
        shell: bash
        run: |
          BIN_PATH="build/pc/${{ matrix.exe_name }}"
          test -f "$BIN_PATH"

          OUTDIR="artifact"
          mkdir -p "$OUTDIR"
          cp "$BIN_PATH" "$OUTDIR/${{ matrix.exe_name }}"

      - name: Create archive
        if: ${{ inputs.os_filter == 'all' || inputs.os_filter == matrix.os_name }}
        shell: pwsh
        run: |
          $archiveBase = "tmc-${{ matrix.region }}-${{ matrix.os_name }}-${{ inputs.version_label }}"
          $archiveName = "$archiveBase.zip"
          if (Test-Path $archiveName) {
            Remove-Item $archiveName -Force
          }
          Compress-Archive -Path artifact\* -DestinationPath $archiveName
          "archive_name=$archiveName" >> $env:GITHUB_ENV

      - name: Upload artifact
        if: ${{ inputs.os_filter == 'all' || inputs.os_filter == matrix.os_name }}
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.archive_name }}
          path: ${{ env.archive_name }}
          if-no-files-found: error
